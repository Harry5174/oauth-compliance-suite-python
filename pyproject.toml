[project]
name = "authlete-python-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-multipart>=0.0.22",
]

[tool.uv.workspace]
members = ["python_oauth_server", "compliance_suite"]
